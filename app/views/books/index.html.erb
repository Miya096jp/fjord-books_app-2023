<p style="color: green"><%= notice %></p>

<h1><%= t('models.book.header.books_index') %></h1>

<div id="books" class="index-items">
  <% @books.each do |book| %>
    <div class="index-item">
      <%= render book %>
      <p>
        <%= link_to t('models.book.link.show_this_book'), book %>
      </p>
    </div>
  <% end %>
</div>

<nav class="page-nav">
  <%= link_to t('models.book.link.new_book'), new_book_path %>
</nav>
