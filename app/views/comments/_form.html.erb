<%= form_with(model: [ commentable, comment ]) do |form| %>

  <div>
    <%= form.label :title %>
    <%# <%= form.text_field :title, required: true %1> %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body %>
    <%# <%= form.text_area :body, required: true %1> %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
